spring:
  application:
    name: user
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888
#      discovery:
#        enabled: true
#        serviceId: configserver #连接的配置中心名称
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/